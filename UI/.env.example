# Angular UI Environment Variables
# This file shows what environment variables can be configured

# For local development, use environment.ts
# For production deployment (Vercel), configure these in:
# Vercel Dashboard -> Project Settings -> Environment Variables

# API Configuration
# The UI will make requests to /api/process automatically
# No additional configuration needed for Vercel deployment

# Optional: Add custom API URL if deploying to different endpoint
# API_URL=https://your-api-domain.com/api/process